import { mount } from '@vue/test-utils'
import {{properCase component}} from '@/components/{{properCase component}}'

describe('{{properCase component}}', () => {
  test('is a Vue instance', () => {
    const wrapper = mount({{properCase component}})
    expect(wrapper.vm).toBeTruthy()
  })
})
